# 云端备份 / 恢复 接入说明（Supabase）

> 本次仅在红线允许的文件内落地：`pubspec.yaml`（追加依赖）、`lib/services/cloud_sync_service.dart`（新建）、`lib/features/card_management/presentation/pages/profile_page.dart`（末尾追加 UI）。
> 现有 Isar 数据模型、本地 CRUD、已有 `cloud_sync_service` stub 均未触碰。

## 1. 依赖
- `pubspec.yaml` 仅追加 `supabase_flutter: ^2.5.0`（实际解析至 2.16.0，API 兼容）。
- 其他现有依赖项**未修改、未升级**。

## 2. 凭证注入（必须，否则功能不可用）
服务通过编译期 `--dart-define` 读取 Supabase 地址与密钥，**缺失即抛明确异常，不会静默失败或崩溃**。

### 2.1 运行 / 调试（flutter run）
```bash
flutter run -d <device> \
  --dart-define=SUPABASE_URL=https://YOUR-PROJECT.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=YOUR-ANON-KEY
```

### 2.2 构建 APK
```bash
flutter build apk --debug --target-platform android-arm64 \
  --dart-define=SUPABASE_URL=https://YOUR-PROJECT.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=YOUR-ANON-KEY
```

### 2.3 Supabase 后台前置
1. 创建项目，复制 Project URL 与 `anon` / `publishable` key。
2. 创建 Storage 存储桶 `user-backups`（建议 Private 私有）。
3. 配置该桶的 RLS 策略（如允许已登录用户读写自己 `{user_id}/` 前缀的对象）；本项目无登录时使用固定 `local-user` 目录，请为该路径放行或放宽策略用于个人备份。

## 3. UI 入口
- 位置：个人页（`profile_page.dart`）底部「数据云端备份」独立卡片。
- 按钮一【立即备份到云端】：静默执行 `uploadBackup()`，完成后 Toast 提示。
- 按钮二【从云端恢复备份】：先弹二次确认（警告覆盖本地数据），确认后执行 `restoreBackup()`。
- Web 端点击会提示「Web 端不支持」。

## 4. 安全防线（实现要点）
- **备份只读**：通过 `Isar.copyToFile` 生成一致性快照副本上传，绝不读运行中的主 `.isar`。
- **临时文件清理**：上传后 `finally` 中必删本地临时副本。
- **回滚点**：恢复前将 `default.isar` 复制为 `default.isar.old`，失败可手动还原。
- **下载校验**：云端文件下载后校验 size > 0 再原子替换主库。
- **Web 禁用**：`kIsWeb` 守卫，`copyToFile` 在 Web 抛 `unsupportedOnWeb`。

## 5. 上线后的必要步骤
恢复完成后建议**重启应用**以重新打开 Isar 实例加载新数据（当前提示文案已引导用户重启）。若要自动化重开，需在主入口处重建 Isar 实例——这超出本次红线范围，需另立项处理。

## 6. 静态检查
- `flutter analyze` 结果：**No issues found!（0 Error / 0 Warning / 0 Info）**。
